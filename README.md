# Prac
Fork--
git clone <repo-url>
cd ClonedFileName
git branch new-branch
git switch/checkout new-branch
git status
make changes in file
git add .
git commint -m "Changes made"
git push origin branch-name
make changes in file
git add .
git commit -m "Changes done"
git push origin other-branch
git diff branch_name (see diffnce)
git merge branch_name (take the - line to add)
pull request done on github mannualy
to show the changes in local file we us pull
git pull origin branch_name
